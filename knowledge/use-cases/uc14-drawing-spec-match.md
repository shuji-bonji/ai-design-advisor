# 設計判断レポート: UC14 図面・仕様書の項目照合

**対象**: 複数 PDF 図面・技術仕様書から項目を抽出し、文書間で一致するかを照合して一覧で返す。
**前提**: 読み取りのみ。知識は入力そのもの（RAG しない）。データを外に出せるかは未確認。合格線は未確認。
**日付**: 2026-08-31
**型**: スキル完全出力（canonical 検証用）
**元**: [memo-b-drawing-spec-match.md](./memo-b-drawing-spec-match.md)

### 推奨構成（要約）

- モデル: 抽出は SLM / 低 Temperature。図面が画像なら Multimodal 入力。外出不可なら D1 が勝つ
- パラメータ: Temperature 0 寄り。Effort は上げない
- 知識: RAG しない。入力文書そのもの
- 実行系: 抽出だけ生成。一致判定は決定的コード。段を上げない
- Fine-tune: しない
- 権限: 読み取り。書き込みなし
- 測り方: 抽出成功と一致判定を分ける。取れない項目を一致にしない

### 次元別

| 次元 | 推奨 | 根拠 | 確度 |
| --- | --- | --- | --- |
| D1 | 抽出は SLM。図面が画像なら Multimodal。外出未確認なら条件付き | selection.md | 条件付き |
| D2 | Temperature 低め。Effort なし | temperature-and-effort.md | 確定 |
| D3 | 入力そのもの。Memory 不要 | knowledge-and-memory | 確定 |
| D4 | 項目名と空欄を軸にする。取れないときの出力を空にする | writing.md | 確定 |
| D5 | RAG しない。取れない項目を一致にしない | when-and-how.md | 確定 |
| D6 | 段 0–1。判定をエージェントにしない | when-to-agentize.md | 確定 |
| D7 | しない | when-to-finetune.md | 確定 |
| D8 | 図面・PDF も入力。注入を疑う。社外渡しは未確認 | threat-landscape.md | 条件付き |
| D9 | 抽出失敗と一致誤りを分ける。見逃し率の数値は持たない | loop-eval-and-stop | 情報不足 |
| X | 数値目標は持たない | serving-and-cache.md | 情報不足 |

### 詳細

- **推奨**: 生成は抽出まで。一致はコード。残る項目は未取得のまま一覧に出す。
- **なぜ**: 正解が決定できる仕事に段を足さない（when-to-agentize）。Grounding と同じで、取れないのに一致と言わせない。
- **代替**: 人が項目を指し、ツールは検索だけ。
- **禁止**: 残黙を一致とする、Agentic RAG、削減率を見逃し率の代わりにする。

### ベンダーに渡す文

- やること: 未取得を別状態で返す、判定のコード化、抽出と照合の分離計測
- やらないこと: 一致判定を LLM に任せる、取れない図面を落とす

### 追加確認

- データを外に出せるか
- 見逃しと誤検出のどちらを先に見るか
- テキスト化できない図面の割合

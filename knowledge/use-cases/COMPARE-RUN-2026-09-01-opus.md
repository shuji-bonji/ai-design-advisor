# 比較実行 2026-09-01 Opus 5 高

手順: [COMPARE.md](./COMPARE.md)。貼る文: [COMPARE-PROMPTS.md](./COMPARE-PROMPTS.md)。
結果は canonical にしない。全文は正本に折り込まない。

素側: Claude Desktop、Opus 5 高、別会話、スキルなし。
スキル側: 同一日別会話。カタログ経由でインストール済み。作業ディレクトリに `knowledge/canonical/` は無かった（`SKILL.md` と `OUTPUT-SKELETON.md` のみ）。

UC01 は同日の先行実行（スキルありは canonical を読めたかどうか未記。素は別会話）。

---

## 一覧

| 入力 | 相手 | 枠が同じか | 発明したか | 腐る数字を引いたか | 一言 |
| --- | --- | --- | --- | --- | --- |
| UC01 | スキル | 同じ | 法令名、2–4文 | いいえ | 穴を先に名指す |
| UC01 | 素 | 否。実装手順が先 | 100–200件、数十秒 TTL、2往復、条番号必須 | ミリ秒 SLO は無い | 未確認は書く。幅と手順で埋める |
| UC06 | スキル | 型は同じ。確定は付けない | 少ない | いいえ | canonical 不在のため Step 3 のみ |
| UC06 | 素 | 否 | llama.cpp / Ollama / vLLM、8GB=7–8B 等 | 量子化と VRAM 目安 | 外に出さないは守る。穴を数字で埋める |
| UC18 | スキル | 型は同じ | 「何秒」は問いに残す | 待機数値は持たない | STT / 生成 / TTS を分ける |
| UC18 | 素 | 否 | CPaaS / Whisper / JSON スキーマ | 待機は未確認。「数値を必ず置く」と書く | 製品家族と状態機械が先 |
| UC20 | スキル | 型は同じ | LoRA を推奨しない | 件数バンドは無い | 手段が先の入力を要否に戻す |
| UC20 | 素 | 否 | Phase 2 で LoRA を1 回、VRAM 数十 GB | 件数は少ない | RAG 先行は重なる。手法名が残る |

---

## スキル側の制約

Claude Desktop のインストール先では、スキルが `knowledge/canonical/` を読めない。
パスが作業ディレクトリ依存だから。プラグインキャッシュにリポジトリ全体があっても、CWD がリポジトリルートでなければ失敗する。

この回のスキル出力は Step 3 の衝突解消だけが依拠。確定を付けていない。見本 UC との一貫性検証としては弱い。

---

## 三点の読み

1. **枠** … 差が出た。素は構成図と Phase が先。D2 Effort・確度列は自分では作らない。
2. **発明** … 差が出た。素はランタイム名・VRAM 目安・LoRA を補う。スキルは手法名を推奨しない。
3. **腐る数字** … UC06 で差が出た。素は 8GB / 7–8B 等を目安として書く。UC18 の素は 300ms は置かないが、待機数値を必須とする。

推奨の一部（外出禁止なら本地、電話は引き継ぎ条件をコード側、FT 前に RAG）は重なる。

---

## 次

- スキルが canonical をプラグイン根から読むよう直す（CWD 依存を外す）
- Fable 5 の UC20 は未実行
- 一枚はまだ作らない

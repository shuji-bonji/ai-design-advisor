# 比較実行 2026-09-01

手順: [COMPARE.md](./COMPARE.md)。結果は canonical にしない。

素側: 同一日の Grok。canonical / SKILL / UC 本文を読まず、対象と前提だけで書いた。
スキル側: 既存 UC 本文（一貫性フィクスチャ）。

制約: 素側と採点側が同一会話にいる。独立した他モデルではない。差が小さく見える可能は残る。

入力は足さない。未確認は未確認のまま。

---

## 見る三点（一覧）

| 入力 | 枠が同じか | 発明したか | 腐る数字を引いたか | 一言 |
| --- | --- | --- | --- | --- |
| UC01 対象+前提 | 否。素はモデル/RAG/実装に寄る。D2 Effort・確度・X が欠ける | 素: チャンク幅・1秒・LoRAを選択肢。スキル: 段の断定はあるが製品名は無い | 素: 目安の 1 秒。スキル: 無い | 枠と穴の名指しが差。推奨自体（RAG+在庫ツール、Agent にしない）は近い |
| UC06 対象+前提 | 否。素は機種選定が先 | 素: 8B / Q4 / 8GB を未確認のまま置く | 素: VRAM 目安。スキル: 持たない | 素は「外に出せない」を守るが、穴を数字で埋める |
| UC18 対象+前提 | 否 | 素: Realtime API 名、300ms、書き込み未確認のまま予約確定を疑う | 素: 300ms。スキル: 待機数値は持たない | 待たせないとき素は製品と SLO から入る |
| UC20 対象+前提 | 否 | 素: LoRA / r / 件数。スキル: 手法は情報不足 | 素: 件数バンド。スキル: 無い | ここで差が一番昇る。素は手法から、スキルは要否から |

---

## 入力と素側（要約）

素側のプロンプトは COMPARE.md の例のまま。以下は返ってきた案の要所だけ。全文は正本に折り込まない。

### UC01

対象: 自社 EC のコンシューマ向け FAQ チャット。返品規約・配送・商品説明。在庫は社内 DB 参照。決済・返金実行はしない。  
前提: データを社外モデルに渡してよい（PII は投入しない）。体感の対話が優先。合格線の数値目標は未確認。

素が言ったこと:

- 規約・商品は RAG。在庫は API。決済ツールは付けない
- クラウドの中〜大。難問は reasoning
- チャンクは 500 トークン前後。ハイブリッド検索
- ツール呼び出しまで。自律ループは不要
- 返信開始 1 秒以内を目安
- 口調が気にならなければ後から LoRA も選択肢
- 正答率と引用率で評価

決まっていない、と書いたのは合格線の数字だけ。体感の 1 秒とチャンク幅は入力に無いのに置いた。

スキル（uc01）: D1–D9+X が揃う。D9 だけ条件付き（数値目標が未確認）。製品名は無い。段 0–2 / Hybrid + Rerank は枠の語彙。

### UC06

対象: 端末または社内完結で動く補助。入力は外に出さない。オフラインを許す。  
前提: データを外に出せないは確定。端末性能・VRAM は未確認。対応機能の最小セットは未確認。

素が言ったこと:

- 雲に出さない。本地の 7B–8B 級、Q4 前後
- 目安は RAM 8GB 機
- 知識は本地の小索引。雲 RAG は使わない
- 機能は絞る。多エージェントにしない
- 通信ツールを持たない

VRAM 未確認のまま 8GB / Q4 を置いた。スキルは落ち幅を持たず、評価してから、と返す。

### UC18

対象: 電話に AI が音声で応答し、用件を聞き取って一次対応する。複雑な要件は有人へ渡す。  
前提: 発話は社外の相手。データ外出は未確認。書き込みの有無は未確認。合格線は未確認。

素が言ったこと:

- 音声 Realtime API 系
- 応答開始 300ms を目安
- 一次受けは短い応答。複雑件は転送
- 予約が要るならツールで確定し、後で人が見る
- 告知と保持期限は法務に確認

外出も書き込みも未確認のまま、製品名と 300ms と「ツールで確定」が先に出た。スキルは待機数値を持たず、書き込みは permission、告知は情報不足。

### UC20

対象: 既存モデルに特定ドメインのテキストで追加学習し、そのドメインでの性能を上げる。  
前提: 使用主体は未確認。学習テキストの権利は未確認。業務の合格線は未確認。

素が言ったこと:

- 継続事前学習より LoRA / SFT から
- 件数は数百から数千
- 権利が未確認なら学習しない
- RAG で足りるかは先に試す

権利と RAG 先行はスキルと重なる。違うのは LoRA と件数をここで名指ししたこと。スキルは要否のみ。手法ノートは使わない。

---

## 三点の読み

1. **枠** … 差が出た。素は次元表を自分で作らない。D2（Temperature と Effort は別）と X（待ちの切り分け）が落ちる。確度列は無い。
2. **発明** … 差が出た。素は「分からないことは推測しない」と言われても、幅・ms・件数・手法名を補う。スキルは穴を ` 情報不足` / `条件付き` に残す。
3. **腐る数字** … 差が出た。素は目安として数値を置く。条番号や 2024 年の市場シェアは引かなかった。

推奨スタックの一部（FAQ は RAG、在庫はツール、外出禁止なら本地）は重なる。Issue #2 の「推奨は置き換えやすい」は、UC01 で特に出た。

---

## スキル側で見えたこと

既存 UC は旧スケルトンのままで、「決まっていないこと」が先に来ない。ベンダー文も短い。
UC01 の「段 0–2」「Hybrid + Rerank」「出力 2–4 文」は、素とは違う種の断定である。新契約で書き直すときの穴。

---

## 次

- 他フロンティアで同じ 4 入力を走らせると、同一会話の偏りが外れる
- UC 本文の書き直しは、確認論点を先にし、段名をベンダー文から外す
- 一枚はまだ作らない
